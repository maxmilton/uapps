{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {},
  "include": [
    "*.config.mjs",
    "packages/*/*.d.ts",
    "packages/*/build.ts",
    "packages/*/src",
    "packages/*/test",
    "scripts/*.bun", // files use hashbang: #!/usr/bin/env bun
    "test",
  ],
  "exclude": ["workers"],
}
