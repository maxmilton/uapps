dialog {
  padding: 0;
  border: 4px solid ${x.color.dark2};
  border-radius: ${x.radius};
  background: ${x.color.background};

  &::backdrop {
    background: ${x.fn.color(x.color.dark1).alpha(0.7)};
  }
}
