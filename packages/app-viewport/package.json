{
  "name": "app-viewport",
  "version": "0.3.1",
  "description": "View live info about your current browser viewport",
  "repository": "maxmilton/uapps",
  "author": "Max Milton <max@happyhackers.xyz>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm run prebuild && NODE_ENV=production node build.mjs",
    "dev": "NODE_ENV=development node build.mjs",
    "prebuild": "rm -rf dist; cp -r static dist",
    "serve": "sirv dist --dev --cors"
  },
  "dependencies": {
    "@ekscss/framework": "0.0.46",
    "stage1": "0.7.2"
  },
  "devDependencies": {
    "ekscss": "0.0.16",
    "esbuild-minify-templates": "0.11.0",
    "esbuild-plugin-ekscss": "0.0.16",
    "git-ref": "0.4.0",
    "lightningcss": "1.21.5",
    "purgecss": "5.0.0",
    "sirv-cli": "2.0.2"
  }
}
