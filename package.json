{
  "private": true,
  "workspaces": [
    "packages/*",
    "workers/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo build",
    "dev": "export NODE_ENV=development; bun run build",
    "lint": "turbo lint:css lint:fmt lint:fmt2 lint:js lint:ts",
    "lint:css": "stylelint '**/*.{css,xcss}'",
    "lint:fmt": "oxfmt --check",
    "lint:fmt2": "biome check",
    "lint:js": "oxlint --format=stylish",
    "lint:ts": "tsc --build --noEmit",
    "test": "bun test --only-failures",
    "test:ci": "bun test --coverage --randomize --rerun-each=3",
    "typegen": "turbo typegen"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@cloudflare/workers-types": "5.20260811.1",
    "@maxmilton/oxlint-config": "0.0.8",
    "@maxmilton/stylelint-config": "0.4.3",
    "@maxmilton/test-utils": "0.0.15",
    "@types/bun": "1.3.14",
    "@types/node": "26.2.0",
    "happy-dom": "20.11.2",
    "oxfmt": "0.63.0",
    "oxlint": "1.78.0",
    "oxlint-tsgolint": "7.0.2001",
    "stylelint": "17.14.1",
    "stylelint-config-standard": "40.0.0",
    "turbo": "2.10.9",
    "typescript": "7.0.2",
    "wrangler": "4.120.1"
  },
  "packageManager": "bun@1.99.0"
}
